/**
 * An esbuild installation
 */
export declare abstract class EsbuildInstallation {
    static detect(): EsbuildInstallation | undefined;
    abstract readonly isLocal: boolean;
    abstract readonly version: string;
}
